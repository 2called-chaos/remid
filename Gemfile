source "https://rubygems.org"

# enable listen gem to actually listen instead of polling (on Windows)
# note: this is for development, ext/mkrf_conf.rb is supposed to do this
#       when installing as a gem
gem "wdm", ">= 0.1.0" if Gem.win_platform?

# Specify your gem's dependencies in remid.gemspec
gemspec
